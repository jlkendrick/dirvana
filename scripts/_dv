#compdef dv

# The partial is the current word being completed
local partial="${words[CURRENT]}"

# Call tool
local completions
completions=("${(@f)$(dv-binary "$partial")}")

# Add the completions to the Zsh completion system
compadd -U -a completions
return 0